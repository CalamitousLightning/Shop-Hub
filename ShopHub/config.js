/* =========================================================
   config.js — ShopHub Marketplace global settings
   Loaded before every other script on every page.
   Change currency/symbol here once and every price display
   across the site (product grid, modal, cart, checkout, orders,
   admin) updates automatically — nothing else to edit.
   ========================================================= */

window.SHOPHUB_CONFIG = {
  currency: "GHS",
  symbol: "₵",
  paystack_currency: "GHS",
};
